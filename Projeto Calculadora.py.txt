from tkinter import *
import tkinter as tk
import random

root = Tk()
root.title('Wack a Button')
root.geometry('500x500+300+300')
root.wm_resizable(width=False, height=False)

tempototal = 1
duracaojogo = 30
pontuacaototal = 0

def mole_button():
    if duracaojogo > 0 :
        button = tk.Button(root, text="Mole" )
        button.place(x= random.randint(1,500),y= random.randint(1,500))
        
linhas = 1
def criar_botao():
    x=random.randint(0,500)
    y=random.randint(0,500)
    button = tk.Button(root, text="Sei lá")
    button.place(x=x, y=y)





label = tk.Label(root, text="Pontuação total")
label.place(x=0, y=0)




root.mainloop()